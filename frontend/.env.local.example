# URL of the deployed Python backend (see ../backend). Server-side only --
# not exposed to the browser, since it's read inside app/api/analyze/route.ts,
# not in client-side code.
BACKEND_URL=https://clearview-backend-test.onrender.com
